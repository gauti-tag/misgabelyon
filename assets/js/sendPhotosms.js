/**
 * script to fetch file name and send it to sms interface
 * @param imageName
 */

/*function getImage(imageName)
{

    var realImage = imageName.replace(/^.*\\/," ");

     alert(realImage);

    $("#view_chat").append();

    $.post(

        sUrl+'Chat',
        {variable : variable},
        function(data){

        })

}*/

